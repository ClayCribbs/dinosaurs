$('#myModal').html("<%= escape_javascript(render partial: 'photos/modal', locals: { photo: @photo } ) %>");
$('#myModal').modal('show')
if ($('#left-side-bar').hasClass('active')) {
  $('.full-screen-image-container').addClass('active')
}

